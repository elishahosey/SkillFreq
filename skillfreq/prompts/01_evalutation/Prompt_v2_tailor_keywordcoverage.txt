I will provide:

1. My base resume(s)
2. A CSV of job descriptions

The CSV may include either:

Evaluation columns:
- Order
- Fit Quality (good_fit / possible_fit / weak_fit)
- Role Lane (target_lane / adjacent_lane / wrong_lane)
- Apply Decision (apply_now / manual_review / skip)
- Reason

Or tracker-style columns such as:
- Company
- Role
- JDText
- Link
- Status
- Notes
- Application
- Location
- Contract?
- Pivotable?
- Resume Variant
- Manual FIT (1-5)
- Fit Reason

---

Your task is to suggest targeted, minimal resume tweaks for EACH job.

DO NOT:
- Re-evaluate Fit Quality
- Override Apply Decision
- Re-rank jobs

You may determine effort_level ONLY from the provided columns.

---

Context about me:

- Backend / data-focused engineer
- Strong in: SQL, data validation, ETL-style work, debugging data pipelines, APIs, integrations
- Experience with: Python, Java, REST APIs, JSON/XML, data workflows, production debugging
- Target roles:
  - Data Engineer
  - Backend Engineer (data-heavy)
  - Integration / ETL / pipeline roles
- Experience level: junior to mid-level (0–4 years)

---

Hard Constraints (DO NOT VIOLATE):

- Do NOT invent tools (AWS, Snowflake, Airflow, Databricks, Informatica, SSIS, ADF, etc.)
- Do NOT exaggerate seniority or architecture ownership
- Do NOT rewrite the full resume
- Do NOT keyword-stuff or add unsupported buzzwords
- Do NOT pretend missing tools are covered if they are not in my resume
- Do NOT add platform/tool names unless they already exist in my resume

---

## 🚫 STRICT TRANSLATION GUARD (HIGH PRIORITY)

When suggesting bullet changes:

Every rewritten bullet MUST map to a specific, real action I actually performed — not a generalized, inferred, or “improved sounding” version.

---

### ❌ DO NOT DO THESE (HARD FAILS)

1. No vague abstractions  
Do NOT use phrases like:
- reporting logic
- backend services
- data solutions
- analytics workflows
- scalable systems
- high-performance pipelines
- data products
- enterprise architecture
- cloud-native architecture
- data platform ownership

If a phrase cannot be tied to a concrete action I performed, DO NOT use it.

---

2. No domain assumptions  
Do NOT introduce domain-specific language unless it clearly exists in my experience.

Examples:
- financial datasets
- marketing analytics
- customer insights
- business intelligence outputs
- manufacturing datasets
- insurance data
- healthcare records
- wealth management data

---

3. No responsibility inflation  
Do NOT upgrade the scope of my work.

Examples:
- helper scripts → pipelines ❌
- debugging → system reliability engineering ❌
- integrations → platform architecture ❌
- SQL queries → data modeling systems ❌
- CI/CD usage → DevOps ownership ❌
- validation checks → governance framework ❌
- data transfer → data platform migration ❌

---

4. No tool/function misrepresentation  

If something was:
- a helper script → call it a helper/validation script
- used for testing → explicitly say testing
- internal-only → keep it scoped as internal use
- a SQL query → call it a SQL query/check
- an interface → call it an interface/data exchange

Do NOT present it as a production system, platform, framework, or architecture if it was not.

---

### ✅ REQUIRED STYLE

All rewritten bullets MUST:

Use concrete, verifiable actions:
- validated
- parsed
- transformed
- debugged
- extracted
- integrated
- reconciled
- tested
- compared
- verified
- investigated

Use specific objects:
- XML / JSON data
- SQL queries
- schemas
- logs
- records
- interfaces
- APIs
- SFTP transfers
- validation checks
- reconciliation checks
- pipelines (ONLY if truly pipelines)

---

## 🔑 SAFE KEYWORD COVERAGE RULE

For each job, identify 3–6 SAFE keywords or phrases from the JD/Reason that match my real experience.

Examples of SAFE keywords if supported by my resume:
- SQL queries
- SQL validation
- reconciliation checks
- data quality checks
- pipeline stages
- pipeline failures
- failed loads
- root-cause analysis
- production support
- logs
- OpenSearch
- validation checkpoints
- follow-up validation checks
- ingestion
- transformation
- schema validation
- XML / JSON records
- APIs
- SFTP
- system-to-system interfaces
- data exchange
- dependency ordering
- scheduled workflows
- failure recovery
- reliable data processing
- pipeline issue resolution

Use these safe keywords when they naturally improve an existing bullet.

Do NOT add unsafe keywords just because they appear in the JD.

Examples of UNSAFE keywords unless already in my resume:
- Snowflake
- AWS
- Glue
- Lambda
- Databricks
- Spark hands-on
- Kafka hands-on
- Airflow hands-on
- Informatica
- SSIS
- ADF
- Tableau
- Power BI
- Kubernetes
- Terraform
- cloud architecture
- platform ownership
- distributed systems ownership
- real-time streaming
- data lake architecture

---

## 🎯 KEYWORD GAP CHECK

For each role with effort_level = medium or high:

Before finalizing, check whether the suggested bullets are missing obvious SAFE role keywords.

Ask:

1. Does the job emphasize data quality, validation, or reconciliation?
   - If yes, at least one suggested bullet should include:
     SQL validation, reconciliation checks, data quality issues, records, or validation checks.

2. Does the job emphasize production support, failed loads, troubleshooting, or RCA?
   - If yes, at least one suggested bullet should include:
     production support, failed loads, pipeline failures, SQL queries, logs, root-cause analysis, or follow-up validation checks.

3. Does the job emphasize pipelines, ingestion, transformation, or workflow reliability?
   - If yes, at least one suggested bullet should include:
     ingestion, transformation, pipeline stages, dependency ordering, validation checkpoints, scheduling, failure recovery, or reliable data processing.

4. Does the job emphasize APIs, integrations, SFTP, or data movement?
   - If yes, at least one suggested bullet should include:
     APIs, SFTP, XML/JSON records, data exchange, interfaces, or data integrity.

Only add these phrases if they are already defensible from my resume.

---

## 🧱 MICRO-TWEAK RULE

If a resume already looks mostly aligned, suggest only a micro-tweak instead of rewriting a full bullet.

Examples:

Current:
- Built multi-step ETL workflows with dependency ordering, validation checkpoints, scheduling, and failure recovery to support reliable data processing

Safe micro-tweak:
- Built multi-step ETL workflows with dependency ordering, validation checkpoints, scheduling, and failure recovery to support reliable data processing and pipeline issue resolution

This is acceptable because “pipeline issue resolution” maps to actual debugging/failure recovery work.

Do NOT force a micro-tweak if the current bullet already has strong keyword coverage.

---

### 🔍 SELF-CHECK (MANDATORY)

Before finalizing EACH bullet:

1. Could I explain this in detail in an interview?
   If NO → rewrite it

2. Does this introduce anything I didn’t explicitly do?
   If YES → remove it

3. Is this phrasing more “impressive” than “accurate”?
   If YES → simplify it

4. Did I generalize something small into something larger?
   If YES → scale it back

5. Did I miss an obvious safe keyword from the JD that matches my real experience?
   If YES → add it naturally, without keyword stuffing

---

### 🧪 EDGE CASE RULES (CRITICAL)

Python helper scripts:
- MUST be described as:
  - helper scripts
  - validation scripts
  - used during testing
- MUST NOT be described as pipelines or production workflows

Integration work:
- MUST be described as:
  - system-to-system interfaces
  - data exchange
  - APIs / SFTP
  - XML / JSON records
- MUST NOT be described as backend services or platforms

SQL work:
- MUST be described as:
  - queries
  - validation
  - reconciliation
  - record comparison
  - test checks
- MUST NOT be described as analytics layers, semantic models, or data products

Debugging work:
- MUST be described as:
  - root-cause analysis
  - investigating failures
  - SQL queries
  - logs
  - OpenSearch
  - verifying corrected records
- MUST NOT be described as:
  - SRE ownership
  - platform reliability engineering
  - cloud operations ownership

Pipeline work:
- MAY be described as:
  - ETL pipelines
  - ingestion
  - transformation
  - validation checkpoints
  - scheduling
  - dependency ordering
  - failure recovery
  - pipeline stages
  - pipeline issue resolution
- MUST NOT be described as:
  - distributed platforms
  - cloud-native systems
  - large-scale architecture
  - high-performance pipelines

---

### 🎯 GOAL OVERRIDE

Optimize for:
INTERVIEW DEFENSIBILITY + SAFE KEYWORD COVERAGE

NOT:
- impressiveness
- abstraction
- buzzwords
- fake tool matching
- perfect keyword mirroring

---

### FINAL SAFEGUARD (CRITICAL)

If a bullet cannot be justified using ONLY my existing resume content:
DO NOT MODIFY IT.

If the only way to match the JD is to add unsupported tools/platforms:
Say so in the risk check instead of adding them.

---

## 🔑 Effort Level (STRICT)

For each row, determine:

effort_level = high / medium / low / none

Use this exact logic:

1. If Apply Decision = skip
   → effort_level = none

2. If Fit Quality = good_fit AND Apply Decision = apply_now AND Role Lane = target_lane
   → effort_level = high

3. If Fit Quality = good_fit AND Role Lane = adjacent_lane
   → effort_level = medium

4. If Fit Quality = good_fit AND Apply Decision = manual_review
   → effort_level = medium

5. If Fit Quality = possible_fit AND Role Lane = target_lane
   → effort_level = medium

6. If Fit Quality = possible_fit AND Role Lane = adjacent_lane
   → effort_level = low

7. If Fit Quality = weak_fit
   → effort_level = none

8. If unsure between two levels
   → choose the LOWER one

---

## ⚙️ Behavior by Effort Level

### effort_level = none
- Output: "No tailoring"
- Do NOT suggest any changes

---

### effort_level = low
- Minimal effort
- At most 1 small wording tweak
- Do NOT try to “fix” the role
- Only add a safe keyword if the current bullet already supports it

---

### effort_level = medium
- Light tailoring
- Modify 1–2 bullets
- Focus on:
  - ETL / pipelines
  - SQL / validation
  - debugging / root-cause analysis
  - integrations / APIs
- Include safe JD keywords where defensible
- Do NOT over-engineer

---

### effort_level = high
- Strongest allowed tailoring
- Modify 2–3 bullets
- Optional: 1 new bullet ONLY if clearly justified
- Focus on:
  - pipeline ownership
  - validation logic
  - debugging production issues
  - integrations / APIs
- Include safe JD keywords where defensible

---

## Tailoring Rules

1. Anchor to EXISTING EXPERIENCE  
- Every change must map directly to something already in my resume  

2. Prefer TRANSLATION over ADDITION  
- Reword bullets instead of adding tools  

3. Be JOB-SPECIFIC  
Use:
- description
- Reason  

Examples:
- pipelines → ingestion / transformation / validation  
- data quality → reconciliation / checks  
- failed loads → production support / pipeline failures  
- troubleshooting → SQL queries / logs / OpenSearch / root-cause analysis  
- backend → APIs / integration flows  

4. Preserve strong signals (CRITICAL)  
- Never remove SQL / ETL / debugging / validation / integration signals  
- Rewrites must be equal or stronger  

5. Do NOT compensate for missing tools  
- If job mentions AWS, Snowflake, Databricks, Kafka, etc. and I don’t have them:
  → do NOT fake alignment  
  → emphasize fundamentals only  
  → mention the tool gap in Risk Check if relevant

6. Keep changes SMALL  
- No full rewrites  
- No narrative changes  
- No broad rebranding

7. Preserve ATS-safe keyword coverage  
- If a safe keyword maps to my actual work, use it naturally.
- Do not stuff multiple keywords into one awkward bullet.
- Prefer one strong, concrete keyword phrase over several vague ones.

---

## Output Format (STRICT)

| Order | Fit Quality | Role Lane | Apply Decision | Effort Level | Tailoring Action |

Tailoring Action:
- high → Full tailoring applied  
- medium → Light tailoring applied  
- low → Minimal tailoring  
- none → No tailoring  

---

Then:

[Job: Order X]

1. Best base resume:
- [short answer]

2. Safe keywords to cover:
- [3–6 safe JD keywords that match my actual resume]
- Do NOT include unsupported tool names here

3. Suggested bullet tweaks:
(If effort_level = none → write "No tailoring")

- BEFORE:
- AFTER:

- BEFORE:
- AFTER:

3. Optional new bullet:
- [or "None"]

4. Alignment summary:
- [bullet]
- [bullet]

5. Risk check:
- [bullet]
- [bullet]

---

## Final Safeguard

- No generic filler language  
- Each change must map to:
  - job description
  - AND my real experience  
- No hallucinated tools  
- No over-investing on medium/low roles  
- If a tweak could apply to any generic DE role → make it more specific using safe keywords
- If a role’s main gap is unsupported tooling, acknowledge the gap instead of trying to hide it
- If the resume already has enough safe keyword coverage, say “No additional tweak needed”

---

Goal:

Act like a pragmatic engineer optimizing effort vs ROI — not trying to perfect every resume.