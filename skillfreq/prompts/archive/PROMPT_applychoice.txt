I will provide a CSV of pre-evaluated job roles.

Each row already includes:
- Order
- Fit Quality (good_fit / possible_fit / weak_fit)
- Role Lane (target_lane / adjacent_lane / wrong_lane)
- Apply Decision (apply_now / manual_review / skip)
- Reason (detailed evaluation)

Your task is NOT to re-evaluate the jobs.

Your task is to decide:
"What should I ACTUALLY apply to right now given limited time and realistic recruiter outcomes?"

---

## CONTEXT

- I am a backend/data-focused engineer
- Strong in: SQL, ETL, data validation, APIs, integrations, debugging
- Target roles: Data Engineer (SQL-heavy), ETL/integration roles, backend data processing
- I am targeting junior–mid level (0–4 years)

---

## DECISION CONSTRAINTS (IMPORTANT)

You MUST follow these:

1. I can only apply to a SMALL number of roles (3–6 max)
2. I want the HIGHEST probability of passing recruiter screening
3. I am NOT optimizing for learning or stretch
4. I am NOT optimizing for “interesting roles”
5. I am optimizing for:
   - realistic resume match
   - recruiter pass likelihood
   - minimal stack gap

---

## FILTERING RULES

You MUST:

- IGNORE all rows where:
  - Fit Quality = weak_fit
  - Apply Decision = skip
  - Role Lane = wrong_lane

- Focus ONLY on:
  - possible_fit + target_lane
  - possible_fit + adjacent_lane (ONLY if strong overlap)

---

## RANKING CRITERIA (VERY IMPORTANT)

From the remaining roles, rank based on:

1. How closely the DAY-TO-DAY work matches:
   - SQL
   - ETL / pipelines
   - data validation / reconciliation
   - integrations / APIs

2. How LOW the risk is from:
   - ecosystem lock-in
   - platform ownership expectations
   - vague or inflated responsibilities

3. How likely a recruiter would say:
   "Yes, this background matches what we need"

---

## HARD PLATFORM GATING RULE (CRITICAL)

If a role appears to require direct hands-on experience with a specific platform/tool:

- Snowflake
- Informatica (IDMC / PowerCenter)
- Databricks
- Palantir
- SAP

AND the job description implies candidates will be filtered based on that experience,

THEN:

- DO NOT place this role in APPLY NOW
- Place it in OPTIONAL / STRETCH at best

IMPORTANT:
Assume recruiters use keyword filtering for these tools.
General SQL/ETL overlap does NOT override this.

---

## TOOL vs SYSTEM RULE (CRITICAL)

Distinguish between:

1. TOOL DIFFERENCE (ACCEPTABLE)
   - Example: Oracle vs SQL Server
   - Same type of work (SQL, ETL, data movement)
   - These roles CAN be APPLY NOW

2. SYSTEM / PLATFORM DIFFERENCE (HIGH RISK)
   - Example: AWS modernization, DataOps platforms, multi-system architecture ownership
   - Involves environment ownership, platform transitions, or cross-system design
   - These roles should be downgraded to OPTIONAL or lower

IMPORTANT:
Prefer roles where the WORK is familiar even if the TOOL is different.
Avoid roles where the SYSTEM or PLATFORM context is unfamiliar.

---

## OUTPUT FORMAT

Give me EXACTLY this:

### APPLY NOW (Top 3–5)
- Order X — 1–2 sentence reason focused on recruiter realism

### OPTIONAL / STRETCH (if I have extra time)
- Order X — 1–2 sentence reason explaining the risk

### DO NOT APPLY
- Brief 1–2 sentence summary of why the rest are not worth it

---

## IMPORTANT

- Be decisive
- Do NOT hedge
- Do NOT repeat the original evaluation
- Do NOT include weak roles just to fill space
- Prefer fewer, higher-quality applications over more

---

## FINAL RULE

If fewer than 3 roles meet strong criteria, return fewer than 3. Do NOT force weak options.